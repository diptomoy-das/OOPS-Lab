import * as ledger from '@midnight-ntwrk/ledger-v7';
import { Effect } from 'effect';
import { WalletError } from '@midnight-ntwrk/wallet-sdk-shielded/dist/v1/WalletError.js';
export interface ProvingService<TTransaction> {
    prove(transaction: ledger.UnprovenTransaction): Effect.Effect<TTransaction, WalletError>;
}
export type DefaultProvingConfiguration = {
    provingServerUrl: URL;
};
export declare const makeDefaultProvingService: (configuration: DefaultProvingConfiguration) => ProvingService<ledger.FinalizedTransaction>;
export declare const makeSimulatorProvingService: () => ProvingService<ledger.ProofErasedTransaction>;
